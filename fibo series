#include <stdio.h>

int main() {
    long n, print;
    printf("Enter a positive integer: ");
    scanf("%ld", &n);

       int a=0,b=1;
       printf("%d%d",a,b);
       for(int i=0;i<n;i++){
           print=a+b;
           printf("%ld,",print);
           a=b;
           b=print;
       }

    return 0;
}
